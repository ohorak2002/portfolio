# assets/

Put your files here:

- `resume.pdf` — linked from the "Download PDF" button and the command palette.
  If you don't have one yet, set `links.resume` to `""` in `js/data.js` and the
  button disappears.
- Project screenshots, an avatar, an OG preview image — anything you reference
  from `js/data.js`.
